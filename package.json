{
  "name": "demo-project",
  "version": "1.0.0",
  "main": "app/index.js",
  "license": "MIT",
  "author": "dazza",
  "type": "module",
  "scripts": {
    "dev": "rimraf dist build && eleventy --serve --quiet",
    "test": "vitest run"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.1",
    "eleventy-plugin-console-plus": "^0.1.1",
    "jsdom": "^26.1.0",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "vitest": "^3.2.1"
  }
}
