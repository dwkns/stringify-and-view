{
  "name": "demo-project",
  "version": "1.0.0",
  "main": "app/index.js",
  "license": "MIT",
  "devDependencies": {
    "vitest": "^3.2.1"
  },
  "author": "dazza",
  "type": "module",
  "scripts": {
    "clean": "npx rimraf dist build",
    "dev": "npm run clean && eleventy --serve --quiet",
    "test": "vitest run"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.1",
    "eleventy-plugin-console-plus": "^0.1.1"
  }
}
